//function parse_region(world_folder, dimension, block_id, state_map, x2, y2, z2) {

	//Find region from coords

	//Find chunk in region

	//Read palette, if block exists use it's ID, otherwise make a new entry and use that.

	//Place block in chunk byte stream


//}
